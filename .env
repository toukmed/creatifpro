# ============================================================
# CreatifPro – Environment Variables
# Replace the placeholder values below with your own secrets
# before deploying. For local overrides, use a .env.local file
# (which is git-ignored and takes precedence).
# ============================================================

# PostgreSQL
POSTGRES_DB=creatif-pro
POSTGRES_USER=toukrichte
POSTGRES_PASSWORD=Collab@7393

# Spring Boot datasource (must match the PostgreSQL values above – same credentials)
SPRING_DATASOURCE_URL=jdbc:postgresql://db:5432/creatif-pro
SPRING_DATASOURCE_USERNAME=toukrichte
SPRING_DATASOURCE_PASSWORD=Collab@7393

# JVM tuning (optional – override the Dockerfile default)
JAVA_OPTS=-XX:+UseContainerSupport -XX:MaxRAMPercentage=75.0
